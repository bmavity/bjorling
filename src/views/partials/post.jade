h2 
  a(href: '#')= post.title

.post
  p #{ post.publishDate } by #{ post.author }
  .content!= post.content
  .comments
    a(href: 'http://www.brianmavity.com/' + post.title.replace(' ', '-') + '/#disqus_thread') Comments
